# Day 6 — Playlist Creation + End-to-End

Full pipeline working: WHOOP state detection → song matching → Spotify playlist push → DB logging.

Built `matching/generator.py` (end-to-end orchestration) and `spotify/playlist.py` (Spotify API interaction). Dynamic playlist names based on neuro profile + recovery: "Slow Down" for fatigue, "Fuel Up" for baseline, "Full Send" for peak readiness.

Era cohesion integrated into matching: genre-aware Gaussian decay on release year (hip-hop σ=2, ghazal σ=12, Bollywood σ=6). Weight tuned to 0.20 (from 0.10) after testing showed 0.10 couldn't exclude cross-era songs.

LLM prompt improved with release_year to help disambiguate songs with common Hindi phrases as titles.

Dry-run tested: baseline state, 20 tracks, mean cohesion 0.612, tight 2010s Bollywood cluster.

794 tests passing.
