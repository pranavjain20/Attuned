# Day 5 — Matching Engine + Essentia Full Library

**Matching engine v1:** Range-box scoring with state-specific target ranges. Real data validation across fatigue/peak/baseline/poor_recovery — all produce intuitively correct playlists.

**Matching engine rewrite (v2):** Replaced range-box with neuro-score dot product. Diagnosed para↔grnd correlation (r=0.921) via 18 diagnostic analyses. Decorrelated grounding formula, added mood tags as 15% weight (semantic dimension orthogonal to audio). Widened state profile gaps. Replaced pool-based selection with unified ranking + recent anchors.

**Essentia full library:** 1,348/1,360 clips downloaded (99.1%). Recovered 197 missing duration_ms from listening history. Full Essentia analysis. Para↔grnd correlation: 0.921 → 0.638.

Key insight: feature correlation is a profiler problem, not a matching problem. The dot product was correct — the profiler's grounding formula was computing from the same features as parasympathetic.

697 → 774 tests passing.
